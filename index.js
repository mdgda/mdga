
//window.alert("se hizo un cambio");
//comentario

/*
comentario
largo
 */

let nombre = "mdga";
document.getElementById("tituloaside").textContent = nombre;

//variables
let x;
x = 100;
console.log(`${x} eso tilin`);


/*document.addEventListener('mousemove', (e) => {

    const mouseX = e.clientX;
    const mouseY = e.clientY;


    const viewportWidth = window.innerWidth;
    const viewportHeight = window.innerHeight;


    const mouseXPercent = (mouseX / viewportWidth) * 100;
    const mouseYPercent = (mouseY / viewportHeight) * 100;


    document.body.style.setProperty('--mouse-x', `${mouseXPercent}%`);
    document.body.style.setProperty('--mouse-y', `${mouseYPercent}%`);

    document.body.style.setProperty('--mouse-x', `${mouseXPercent}%`);
    document.body.style.setProperty('--mouse-y', `${mouseYPercent}%`);
});

 */
